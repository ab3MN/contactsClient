import React from 'react';
import DateBookList from './DateBookList/DateBookList';

const DateBooks = () => {
  return (
    <section>
      <DateBookList />
    </section>
  );
};

export default DateBooks;
